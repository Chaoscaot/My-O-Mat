import { AppOverviewPage } from "@/components/omat/app-overview-page"
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Arbeitsbereich",
}

export default function Page() {
  return <AppOverviewPage />
}
